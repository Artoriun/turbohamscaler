<!-- What changed, and why it needed to. If it fixes something, say what the symptom was —
     that is what makes the commit readable in a year. -->

## What this changes

## How it was checked

<!-- `npm run ci` is the pipeline CI runs, in CI's order. If this fixes a bug, the useful thing
     is a test that fails without the fix: a check nobody has watched fail is not evidence. -->

- [ ] `npm run ci` passes
- [ ] New behaviour has a test, and I have seen it fail on the bug it covers
