{
  "name": "@hamscaler/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "node --disable-warning=ExperimentalWarning --watch --import tsx src/index.ts",
    "build": "tsc -p tsconfig.build.json && node scripts/copy-migrations.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hamscaler/shared": "*",
    "@hono/node-server": "^2.1.1",
    "hono": "^4.13.2"
  },
  "devDependencies": {
    "@types/node": "^26.2.0"
  }
}
