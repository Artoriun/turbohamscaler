# Everything here is public and safe to index. The signed-in app is behind a session, so a
# crawler reaching /app sees the same "no API behind this copy" page a visitor would.
User-agent: *
Allow: /
